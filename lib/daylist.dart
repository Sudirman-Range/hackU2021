import 'package:flutter/material.dart';

class DayList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('日付によっての状態を保持したリストを表示'),
    );
  }
}
